import React from "react";

const PlayButton = () => {
    return (
        <React.Fragment>
            <button type="submit">New Game</button>
        </React.Fragment>
    )
}

export default PlayButton;